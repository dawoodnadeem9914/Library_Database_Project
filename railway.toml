[build]
builder = "RAILPACK"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -Dspring.profiles.active=${SPRING_PROFILES_ACTIVE:-railway} -jar target/library-pro-2.0.0.jar"
healthcheckPath = "/login"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
